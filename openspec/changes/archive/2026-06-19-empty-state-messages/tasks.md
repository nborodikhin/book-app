## 1. UI — SearchScreen

- [x] 1.1 In the `Success` branch of `SearchScreenContent`, add an empty-state check: when `state.results.isEmpty()`, show a centred "No results" `Text` instead of the `LazyColumn`

## 2. Preview

- [x] 2.1 Add `SearchEmptyPreview` showing `Success` state with an empty results list

## 3. Tests

- [x] 3.1 `SearchScreenTest`: add test — `Success` state with empty results renders "No results" text
